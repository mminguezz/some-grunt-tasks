module.exports = function( grunt ) {
/*   
    config = {
        // https://github.com/gruntjs/grunt-contrib-xxxx
        xxxx: {
            options: { 
                banner: "<%= pkg.name %> v<%= pkg.version %> \n",
            }
        }
    };
    
    grunt.config.merge(config);

    grunt.loadNpmTasks('grunt-contrib-xxxx');

    grunt.registerTask(
          'xxxx'
        , 'lorem xxxx ipsum dolor, ej.:'
           +'\n                             ' 
           +'grunt xxxx'['yellow']
           +'\n '
        , [
            'xxxx'
        ]
    );
*/
}